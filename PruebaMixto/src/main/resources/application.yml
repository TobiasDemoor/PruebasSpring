spring:
    profiles:
        active: '@spring.profiles.active@'
    datasource:
        url: jdbc:h2:mem:testDB
        driver-class-name: org.h2.Driver
        username: sa
        password: password
    h2:
        console:
            enabled: true
            path: /h2-console

    jpa:
        show-sql: false