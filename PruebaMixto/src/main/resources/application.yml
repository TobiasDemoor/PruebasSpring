spring:
    profiles:
        active: '@spring.profiles.active@'
    datasource:
        url: jdbc:h2:mem:testDB
        driver-class-name: org.h2.Driver
        username: sa
        password:
    h2:
        console:
            enabled: true
            path: /console

    jpa:
        show-sql: false